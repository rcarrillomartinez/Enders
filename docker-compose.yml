version: '3.8'

services:
  # Servicio Web con Apache y PHP
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: enders-web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker/vhost.conf:/etc/apache2/sites-available/000-default.conf
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_NAME=wordpress6
      - DB_USER=wordpress6
      - DB_PASSWORD=ZApzis0YqfKOY2Pw

  # Servicio de Base de Datos (MariaDB)
  db:
    image: mariadb:10.6
    container_name: enders-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: wordpress6
      MYSQL_DATABASE: wordpress6
      MYSQL_USER: wordpress6
      MYSQL_PASSWORD: ZApzis0YqfKOY2Pw
    volumes:
      - db_data:/var/lib/mysql

  # phpMyAdmin para gestionar la base de datos
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: enders-phpmyadmin
    ports:
      - "8081:80"
    environment:
      PMA_HOST: db
    depends_on:
      - db

volumes:
  db_data:

